# Cache npm cũ của máy bị file thuộc quyền root (do từng chạy sudo npm) → trỏ sang cache riêng.
# Có thể xoá 2 dòng này sau khi sửa quyền: sudo chown -R $(whoami) ~/.npm
cache=${HOME}/.npm-clean-cache
legacy-peer-deps=true
